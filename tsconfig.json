{
  "compilerOptions": {
    "target": "ES6",
    "module": "commonjs",
    "outDir": "./dist",  // The compiled JavaScript will go here
    "rootDir": "./src",  // Your source TypeScript code is in the src folder
    "strict": true,  // Enforces strict type-checking options
    "esModuleInterop": true  // Allows default imports to work for non-ESM modules
  },
  "include": [
    "src/**/*"  // Includes all files in the src directory
  ]
}
